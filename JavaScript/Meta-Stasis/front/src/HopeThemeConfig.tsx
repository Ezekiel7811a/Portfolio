import { HopeThemeConfig } from "@hope-ui/solid";

const config: HopeThemeConfig = {
  components: {
    Input: {
      baseStyle: {
        input: {
          color: "white",
          margin: "1%",
        },
      },
    },
  },
};

export default config;
